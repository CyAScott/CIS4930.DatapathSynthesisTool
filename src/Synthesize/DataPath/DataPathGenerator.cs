using System.IO;
using Synthesize.Multiplexor;

namespace Synthesize.DataPath
{
    public class DataPathGenerator
    {
        public DataPathGenerator(MultiplexorGenerator multiplexors)
        {
        }
        public void SaveDataPath(StreamWriter stream)
        {
        }
        public void SaveController(StreamWriter stream)
        {
        }
        public void SaveDesign(StreamWriter stream)
        {
        }
        public void SaveTestBench(StreamWriter stream)
        {
        }
    }
}
